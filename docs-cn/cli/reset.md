---
summary: "`moltbot reset` CLI 参考(重置本地状态/配置)"
read_when:
  - 您想擦除本地状态,同时保持 CLI 已安装
  - 您想对将要删除的内容进行试运行
---

# `moltbot reset`

重置本地配置/状态(保持 CLI 已安装)。

```bash
moltbot reset
moltbot reset --dry-run
moltbot reset --scope config+creds+sessions --yes --non-interactive
```
