---
summary: "`moltbot sessions` CLI 参考(列出存储的会话 + 使用情况)"
read_when:
  - 您想列出存储的会话并查看最近的活动
---

# `moltbot sessions`

列出存储的对话会话。

```bash
moltbot sessions
moltbot sessions --active 120
moltbot sessions --json
```
