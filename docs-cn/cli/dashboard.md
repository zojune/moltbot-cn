---
summary: "`moltbot dashboard` CLI 参考(打开控制 UI)"
read_when:
  - 您想使用当前令牌打开控制 UI
  - 您想在不启动浏览器的情况下打印 URL
---

# `moltbot dashboard`

使用您当前的认证打开控制 UI。

```bash
moltbot dashboard
moltbot dashboard --no-open
```
